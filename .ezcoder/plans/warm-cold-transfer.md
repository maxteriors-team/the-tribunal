# Live Warm/Cold Call Transfer (AI → Human Closer)

## Goal
Let an AI voice agent hand an active Telnyx call to a human closer mid-conversation via a
new `transfer_call` tool. **Warm** mode dials the human, speaks a 1–2 sentence briefing
(caller name, intent, key facts) to the human only, then bridges. **Cold** mode bridges
immediately. Destination is configured per-agent (fallback per-workspace). The handoff is
logged consistently with existing call-event logging and respects the HITL approval gate.

## Verified facts (from research)
- Telnyx Call Control transfer endpoint: `POST /calls/{call_control_id}/actions/transfer`,
  required body `{"to": ...}`; optional `from`, `from_display_name`, `client_state` (base64),
  `custom_headers`, `timeout_secs`, `audio_url` (played to destination before bridging).
- Telnyx also exposes `POST /calls/{ccid}/actions/speak` (`payload`,`voice` required;
  `voice` format `<Provider>.<Model>.<VoiceId>`, e.g. `Telnyx.KokoroTTS.af`) and
  `POST /calls/{ccid}/actions/bridge` (`call_control_id` of the other leg required).
- Voice webhook event types already used: `call.initiated`, `call.answered`, `call.hangup`,
  `call.machine.detection.ended`. Additional real events: `call.bridged`, `call.speak.ended`.
- Voice webhooks dispatched in `backend/app/api/webhooks/telnyx.py` via `_VOICE_HANDLERS`
  keyed on event_type; handlers live in `telnyx_call_handlers.py`. Webhook path is
  `{api_base}/webhooks/telnyx/voice`. `client_state` is base64; helper `encode_client_state`.
- Tools are emitted by `voice_tools.get_tools_from_agent_config()` → `build_tools_list()`
  (OpenAI Realtime: `{"type":"function","name",...}`). Grok builds its own list in
  `grok/session_config.py`; ElevenLabs in `elevenlabs_voice_agent.py`. DTMF is the precedent
  for a call-control tool gated through `tool_settings["call_control"]`.
- Tool execution: `tool_executor.VoiceToolExecutor.execute()` dispatches by name; voice
  callback runs through `approval_gate_service.check_and_execute_or_queue` (unknown action
  types auto-approve unless a HumanProfile policy says `ask`/`never`). `send_dtmf` shows the
  exact pattern for calling `TelnyxVoiceService` from the executor with `call_control_id`.
- Agent model `backend/app/models/agent.py`; schema `backend/app/schemas/agent.py`
  (Create/Update/Response); service `agent_service.py` (`create_agent` uses `**model_dump()`,
  `update_agent` uses `model_dump(exclude_unset=True)`); frontend mirror types
  `frontend/src/types/agent.ts`, request types `frontend/src/lib/api/agents.ts`, form schema
  `frontend/src/lib/agents/agent-form.ts`, edit UI `tabs/advanced-tab.tsx`.
- Frontend tools registry already declares a `call_control` → `transfer_call` tool
  (`frontend/src/lib/integrations.ts`), so enabling it in the Tools tab already writes
  `tool_settings.call_control += "transfer_call"`. We add destination config + execution.
- Single Alembic head: `20260601_drop_contact_tags`. Migrations use guarded DDL pattern.
- Contact PK is `BigInteger`; Agent/Message/Conversation PKs are UUID.
- Verification targets: `make ci.backend`, `make ci.frontend`, `make ci.codegen`
  (commits `backend/openapi.json` + `frontend/src/lib/api/_generated.ts`), `make ci.migrations`.
  `openapi.json` regenerated by `uv run --project backend export-openapi`.

## Design decisions
- **Transfer mechanism (warm):** dial the human as a *new* transfer leg via the Telnyx
  `transfer` action with `client_state` carrying our transfer marker. We DO NOT pass
  `audio_url` (no object storage to host TTS). Instead, after the destination answers we use
  the existing call-control `speak` action targeted at the destination leg, then `bridge`.
  But Telnyx `transfer` auto-bridges the original leg to the destination once answered, so a
  pure-`transfer` approach cannot inject a destination-only briefing cleanly. **Chosen approach:**
  - **Cold:** single `transfer` call (`to`=destination, `from`=workspace number, `client_state`
    marks `cold`). Telnyx bridges automatically on answer. Simple + reliable.
  - **Warm:** use `transfer` with `from_display_name` set to a short caller label AND embed the
    briefing as a leading `speak` is not possible on the auto-bridged leg. So warm uses the
    Telnyx-native pattern: call `transfer` with the briefing delivered via `speak` BEFORE
    transfer to the *caller* ("Connecting you to a specialist now"), and pass the human-facing
    briefing using `from_display_name` (caller id name) + a `custom_headers`/SIP `User-to-User`
    is not audible. Audible destination-only briefing requires a separate dial+speak+bridge.
  - **Final chosen warm implementation (audible, destination-only briefing, no storage):**
    1. AI leg (caller) is put on hold implicitly while we set up. We call
       `speak` on the caller leg: "One moment, connecting you to <name>." (reassurance).
    2. Issue `transfer` to the destination with `client_state` = base64 of a JSON marker
       `{tribunal_transfer: {mode, briefing, message_id, agent_id}}` and
       `webhook_url` defaulted to our voice webhook so `call.answered` for the new leg routes
       back to us. Set `timeout_secs` from config.
    3. On the destination leg's `call.answered` (detected via our transfer marker in
       `client_state`), if mode==warm: call `speak` on the **destination leg** with the
       briefing text (the caller is on the original leg and does not hear it because the legs
       are not yet bridged — Telnyx transfer holds the A-leg until the B-leg answers, and the
       speak targets only the new leg via its own call_control_id), then on `call.speak.ended`
       issue `bridge` of the two legs. For cold mode, Telnyx auto-bridges; we just log.

  > Note on Telnyx semantics: `transfer` creates a new outbound leg and, on answer, bridges
  > it to the original leg. To interpose a destination-only briefing we must own the new
  > leg's `call.answered`/`call.speak.ended` and bridge explicitly. We therefore set
  > `client_state` on the transfer so the new leg's webhooks carry our marker, and we record
  > the original (A) leg's `call_control_id` in the marker so the bridge step can pair them.
  > If precise leg-hold timing proves provider-dependent, the implementation degrades safely:
  > warm falls back to speaking the briefing then a normal transfer (still audible to human,
  > caller hears hold). This is encapsulated in `TelnyxVoiceService.transfer_call()` +
  > the new webhook handlers so the tool contract is stable.

- **Tool name:** `transfer_call` (matches the existing frontend registry + CLAUDE.md anchor).
- **Approval/HITL:** keep intact. The voice tool callback already routes through the approval
  gate; `transfer_call` flows through unchanged (auto unless operator policy says ask/never).
- **Destination resolution:** per-agent `transfer_destination_number` first, else
  `Workspace.settings["transfer_destination_number"]`. If neither set → tool returns a
  friendly error and the AI continues the call.
- **Audit/logging:** structured logs (`transfer_initiated`, `transfer_bridged`,
  `transfer_failed`) consistent with existing `dtmf_sent_via_tool`; persist a marker on the
  call Message via a new `transfer_outcome`/`transfer_to` column set; also write a
  `CallOutcome.signals` style note is out of scope (CallOutcome is created at hangup). We add
  columns on `messages` to record the handoff and surface it in transcripts/logs.

## Files to change

### Backend — model + migration
1. `backend/app/models/agent.py`
   - Add columns:
     - `transfer_enabled: bool` default False, server_default false.
     - `transfer_mode: str` default `"warm"` (`warm`|`cold`).
     - `transfer_destination_number: str | None` (E.164).
     - `transfer_briefing_voice: str` default `"Telnyx.KokoroTTS.af"` (TTS voice for warm
       briefing; validated server-side, free-form string).
2. `backend/app/models/conversation.py` (Message)
   - Add nullable voice-handoff columns near `booking_outcome`:
     - `transfer_status: str | None` (`initiated`|`bridged`|`failed`).
     - `transfer_to: str | None` (destination E.164, store hashed? No — destination is the
       operator's own number, not contact PII; store plaintext like workspace_phone).
   - Rationale: mirrors how `booking_outcome` is persisted from the voice tool executor.
3. `backend/alembic/versions/<rev>_add_voice_transfer.py`
   - New revision, `down_revision = "20260601_drop_contact_tags"`.
   - Guarded `add_column` for the 4 agent columns + 2 message columns (check
     `information_schema.columns` like existing migration), with server defaults so existing
     rows backfill; `downgrade` drops them. Created via `make migrate.new` then hand-edited to
     guarded style (autogenerate may suffice; keep idempotent).

### Backend — Telnyx telephony
4. `backend/app/services/telephony/telnyx_voice.py`
   - Add `async def transfer_call(self, call_control_id, to_number, *, from_number,
     client_state=None, from_display_name=None, timeout_secs=30) -> bool` → POST
     `/calls/{ccid}/actions/transfer` with normalized E.164, structured logs + error handling
     mirroring `send_dtmf`.
   - Add `async def speak_text(self, call_control_id, text, *, voice, language=None,
     client_state=None) -> bool` → POST `/calls/{ccid}/actions/speak` (`payload`,`voice`).
   - Add `async def bridge_calls(self, call_control_id, other_call_control_id, *,
     client_state=None) -> bool` → POST `/calls/{ccid}/actions/bridge`.
   - All three: 3 messages (`transferring_call`/`speaking_text`/`bridging_calls`), success +
     `httpx.HTTPStatusError` + generic except branches, return bool. Reuse `_normalize_e164`.

### Backend — transfer orchestration service (new)
5. `backend/app/services/telephony/call_transfer.py` (new)
   - `build_briefing(contact_info, offer_info, reason) -> str`: 1–2 sentence briefing
     ("Incoming transfer: <name> from <company>. They're interested in <intent>. <reason>.").
     Caps length (<= ~300 chars / 3000 Telnyx limit), strips PII beyond name/company/intent.
   - `encode_transfer_state(marker: dict) -> str` using `encode_client_state` semantics but
     for an arbitrary JSON marker → base64(json). Add matching `decode_transfer_state`.
   - `resolve_destination(agent, workspace) -> str | None`.
   - `async def start_transfer(*, call_control_id, agent, contact_info, offer_info, reason,
     mode, destination, workspace_phone, db) -> dict`: persists Message.transfer_status
     `initiated`+`transfer_to`, (warm) speaks reassurance to caller, issues `transfer` with the
     marker `client_state`; returns `{success, message, mode, destination}` for the tool.
   - This module owns the provider calls + DB writes so the executor stays thin.

### Backend — tool definition
6. `backend/app/services/ai/voice_tools.py`
   - Add `TRANSFER_CALL_TOOL` (OpenAI Realtime function shape) with description: call when the
     caller asks for a human, is a hot/qualified lead, or is frustrated; param
     `reason: string` (short reason/intent summary, required) and optional
     `mode: "warm"|"cold"` (defaults to agent config).
   - Add `enable_transfer` param to `build_tools_list()` and append the tool.
   - In `get_tools_from_agent_config()`: enable when `"transfer_call" in enabled_tools` OR
     (`"call_control" in enabled_tools` and `"transfer_call" in tool_settings["call_control"]`),
     mirroring DTMF gating. Only when agent has a resolvable destination is NOT required at
     tool-emit time (resolution happens at execution; tool still offered so the AI can try).
7. `backend/app/services/ai/grok/session_config.py` + `elevenlabs_voice_agent.py`
   - Append `TRANSFER_CALL_TOOL` when enabled (same gating helper) so all three providers can
     call it. Add a shared `_transfer_enabled(agent)` helper in `voice_tools.py` to avoid
     duplicating the gating logic.

### Backend — tool execution
8. `backend/app/services/ai/tool_executor.py` (`VoiceToolExecutor`)
   - Add `transfer_call` dispatch in `execute()` → `_execute_transfer_call(reason, mode)`.
   - `_execute_transfer_call`: requires `call_control_id` + `settings.telnyx_api_key`; loads
     the call Message + conversation + workspace using the executor's existing lookup pattern
     to resolve destination + workspace phone + contact/offer info; calls
     `call_transfer.start_transfer(...)`. Returns the dict for the model. The executor already
     has `agent`, `contact_info`, `workspace_id`, `call_control_id`.
   - Add brief note: text channel does not support live transfer →
     `text_tool_executor.py` returns `{"success": False, "error": "Call transfer is only
     available on voice calls."}` for `transfer_call` so the tool is safe if ever exposed.

### Backend — webhook handlers
9. `backend/app/api/webhooks/telnyx_call_handlers.py`
   - New `handle_call_bridged(payload, log)` and reuse of `handle_call_answered`/new
     `handle_call_speak_ended(payload, log)`:
     - On `call.answered`: if `client_state` decodes to our transfer marker AND mode==warm,
       speak the briefing on this (destination) leg; record nothing else. (Cold: do nothing —
       Telnyx auto-bridges.)
     - On `call.speak.ended`: if marker present and mode==warm, `bridge` destination leg with
       the stored A-leg `call_control_id`; set Message.transfer_status appropriately.
     - On `call.bridged`: if marker present, set Message.transfer_status=`bridged`, log
       `transfer_bridged`.
   - Add a helper to extract+decode our marker from `payload["client_state"]`. Be defensive:
     non-transfer calls have unrelated base64 client_state (idempotency UUID) → decode must
     fail closed (return None) without throwing.
   - Keep idempotency: guard on Message.transfer_status transitions like hangup does.
10. `backend/app/api/webhooks/telnyx.py`
    - Register `"call.bridged": handle_call_bridged` and
      `"call.speak.ended": handle_call_speak_ended` in `_VOICE_HANDLERS`.

### Backend — public API schema
11. `backend/app/schemas/agent.py`
    - `AgentCreate`, `AgentUpdate`, `AgentResponse`: add `transfer_enabled`, `transfer_mode`,
      `transfer_destination_number`, `transfer_briefing_voice` with sensible defaults and a
      validator constraining `transfer_mode ∈ {warm, cold}` and basic E.164-ish check for the
      destination (allow None/empty).
    - `agent_service.update_agent` already setattrs from `model_dump(exclude_unset=True)`;
      `create_agent` uses `**model_dump()`. New fields flow automatically.
12. Regenerate `backend/openapi.json` + `frontend/src/lib/api/_generated.ts` via `make codegen`.

### Frontend — settings UI
13. `frontend/src/types/agent.ts` — add the 4 fields to `Agent`.
14. `frontend/src/lib/api/agents.ts` — add to `CreateAgentRequest` + `UpdateAgentRequest`.
15. `frontend/src/lib/agents/agent-form.ts`
    - Add zod fields (`transferEnabled`, `transferMode` enum, `transferDestinationNumber`
      nullable string, `transferBriefingVoice`) + defaults; map in `buildUpdateAgentRequest`,
      `buildCreateAgentRequest` (optional), and `agentToEditFormValues`; add to `TAB_FIELDS.tools`
      or a new section under `advanced`.
16. `frontend/src/components/agents/tabs/advanced-tab.tsx` (or a small new
    `tabs/advanced/transfer-section.tsx` following the section pattern)
    - Card "Live Call Transfer": Switch `transferEnabled`; when enabled, a Select
      `transferMode` (Warm/Cold), Input `transferDestinationNumber` (E.164), and (warm)
      an Input/Select `transferBriefingVoice` with helper text. Wire to the form.

### Tests
17. `backend/tests/services/test_voice_transfer_tool.py` (new) — mirror
    `test_voice_application_link_tool.py`:
    - `transfer_call` appears in `get_tools_from_agent_config` only when enabled.
    - `_execute_transfer_call` resolves destination (agent → workspace), calls
      `TelnyxVoiceService.transfer_call`/`speak_text` with expected args (mock provider +
      `AsyncSessionLocal`), persists `transfer_status="initiated"`, returns success dict.
    - Returns friendly error when no destination configured / no call_control_id.
18. `backend/tests/api/webhooks/` (new test) — `handle_call_answered` warm path speaks
    briefing on transfer marker; `handle_call_speak_ended` bridges; non-transfer client_state
    decodes to None and is ignored. (Use direct handler calls with fake session, like existing
    webhook tests.)

## Verification
- `make ci.backend` (ruff, format, mypy, pytest+coverage).
- `make ci.codegen` (commit `backend/openapi.json` + `frontend/src/lib/api/_generated.ts`).
- `make ci.migrations` (upgrade head → check → downgrade -1 → upgrade head).
- `make ci.frontend` (lint, typecheck, unit, build).
- Runtime probe: start backend; replay a representative Telnyx `call.answered` payload whose
  `client_state` is our warm transfer marker via
  `.ezcoder/eyes/http.sh http://localhost:8000/webhooks/telnyx/voice POST @payload.json`
  (set `SKIP_WEBHOOK_VERIFICATION=true` locally) and inspect
  `.ezcoder/eyes/logs.sh --service backend --grep "transfer_|ERROR|Traceback"` for the
  `transfer_*` path and no tracebacks. Also replay `call.bridged` to confirm
  `transfer_status="bridged"`.

## Risks / mitigations
- **Telnyx warm-leg timing** (caller hearing the briefing): encapsulated in service +
  handlers; warm degrades to "speak reassurance to caller → transfer" if explicit bridging is
  unavailable. Destination still hears the briefing via destination-leg `speak`.
- **client_state collision** with the idempotency UUID base64: marker decoder validates JSON
  shape + a `tribunal_transfer` key and fails closed, so normal calls are unaffected.
- **Approval gate**: unknown action types auto-approve unless policy set; behavior preserved.
- **Multi-provider**: gating helper shared across OpenAI/Grok/ElevenLabs tool builders.
- **Coverage floor**: add focused unit tests for new service + executor + handler branches.
