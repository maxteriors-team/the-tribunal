"""Operational scripts (one-shot tasks, schema exports, backfills).

Packaged so entry points declared in ``pyproject.toml`` (e.g. ``export-openapi``)
can resolve to ``scripts.<module>:<function>``.
"""
